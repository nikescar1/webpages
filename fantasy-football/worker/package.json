{
  "name": "ff-league-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Cloudflare Worker backend for the fantasy football league app",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "check": "wrangler deploy --dry-run --outdir=.build"
  },
  "devDependencies": {
    "wrangler": "^4.20.0"
  }
}
